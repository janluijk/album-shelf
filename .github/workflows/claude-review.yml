name: Claude Review

on:
  pull_request:
    types: [opened, ready_for_review]

jobs:
  review:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          prompt: |
            Review pull request #${{ github.event.pull_request.number }} in
            ${{ github.repository }} for correctness bugs, security issues
            (especially around auth and data ownership), and deviations from
            the conventions in CLAUDE.md. Be concise: only flag things that
            matter, with file and line references. If everything looks good,
            say so briefly.

            You MUST post your review as a comment on the pull request using:
            gh pr comment ${{ github.event.pull_request.number }} --body "..."
            Your review is not delivered unless that command succeeds.
          claude_args: |
            --allowedTools "Bash(gh pr:*),Bash(gh api:*),Read,Grep,Glob"
            --max-turns 15
